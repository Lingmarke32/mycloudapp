name: Docker CI
on: [push]
jobs:
build:
runs-on: ubuntu-latest
steps:
-name: checkout code
run: actions/checkout@v3
-name: Build Docker Image
run: docker build -t mycloudapp .
-name: Run Container Test
run: docker run -d -p 8080:80 mycloudapp
-name: Test with curl
run: curl http://localhost:8080
